import styles from "./Logo.module.scss";

const Logo = () => {
  return (
    <div className={styles.container}>
      <h1>
        DAW<span>FLIX</span>
      </h1>
    </div>
  );
};

export default Logo;
